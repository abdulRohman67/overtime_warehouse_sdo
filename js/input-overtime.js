import{db}from"./firebase.js";import{ref,onValue,push,set,remove,get}from"https://www.gstatic.com/firebasejs/12.17.1/firebase-database.js";import{requireLogin}from"./nav.js";import{showMsg,esc}from"./session.js";
const s=requireLogin();if(!s||s.role!=="admin"){location.href="dashboard.html"}let all=[],users={};
onValue(ref(db,"users"),snap=>{users=snap.exists()?snap.val():{};userSap.innerHTML='<option value="">Pilih user</option>';Object.entries(users).filter(([k,u])=>u.role==="user").sort().forEach(([k,u])=>userSap.insertAdjacentHTML("beforeend",`<option value="${esc(k)}">${esc(k)} - ${esc(u.name)}</option>`));render()});
onValue(ref(db,"overtime"),snap=>{all=snap.exists()?Object.entries(snap.val()).map(([id,x])=>({id,...x})):[];render()});
function render(){const q=(search.value||"").toLowerCase();const d=all.filter(x=>!q||`${x.userSap} ${users[x.userSap]?.name||""} ${x.date} ${x.note||""}`.toLowerCase().includes(q)).sort((a,b)=>(b.date||"").localeCompare(a.date||""));rows.innerHTML=d.length?d.map(x=>`<tr><td>${esc(x.date)}</td><td>${esc(x.userSap)}</td><td>${esc(users[x.userSap]?.name||"")}</td><td>${esc(x.start)}</td><td>${esc(x.end)}</td><td>${x.hours||0}</td><td>${esc(x.note)}</td><td class="actions"><button data-edit="${x.id}">Edit</button><button class="danger" data-del="${x.id}">Hapus</button></td></tr>`).join(""):'<tr><td colspan="8" class="empty">Belum ada data.</td></tr>'}
search.oninput=render;
otForm.onsubmit=async e=>{e.preventDefault();if(Number(hours.value)<0)return;const data={date:date.value,start:start.value,end:end.value,hours:Number(hours.value),userSap:userSap.value,note:note.value.trim(),updatedAt:Date.now()};const id=editId.value||push(ref(db,"overtime")).key;await set(ref(db,"overtime/"+id),data);showMsg(msg,"Data overtime berhasil disimpan.","ok");reset()};
function reset(){otForm.reset();editId.value=""}resetBtn.onclick=reset;
rows.onclick=async e=>{const ed=e.target.dataset.edit,del=e.target.dataset.del;if(del){if(confirm("Hapus data overtime ini?"))await remove(ref(db,"overtime/"+del));return}if(ed){const x=(await get(ref(db,"overtime/"+ed))).val();editId.value=ed;date.value=x.date||"";start.value=x.start||"";end.value=x.end||"";hours.value=x.hours||0;userSap.value=x.userSap||"";note.value=x.note||"";window.scrollTo({top:0,behavior:"smooth"})}};